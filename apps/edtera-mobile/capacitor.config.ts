import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'edtera-mobile',
  webDir: '../../dist/apps/edtera-mobile',
  bundledWebRuntime: false,
};

export default config;
